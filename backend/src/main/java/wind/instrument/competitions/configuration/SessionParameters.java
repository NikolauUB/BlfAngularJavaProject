package wind.instrument.competitions.configuration;

/**
 * All session parameter names are here
 */
public enum SessionParameters {
    USERNAME,
    USER_CREATED,
    USER_ID,
    QUESTION_ANSWERS;
}
